# Content audit — 2026-05-16

Generated by `packages/db/scripts/content-audit.ts`. REPORT-ONLY — no rows were modified.

## Summary

- PUBLISHED tutorials audited: 538
- Voice-check / structural errors: 51
- Warnings: 618
- Tutorials with no hero Media: 536
- Tutorials marked freezable but missing freezeNotes: 0
- Inline glossaryTooltip refs that don't resolve: 0

## Error counts by kind

- `servings-yield`: 51

## Warning counts by kind

- `americanism`: 74
- `brand-trademark`: 14
- `temperature-canonical`: 1
- `tricolon`: 529

## Per-tutorial errors (first 150)

### `air-fryer-falafel` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `baba-ganoush` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `beef-enchiladas` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `bread-and-butter-pickles` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `bubble-and-squeak` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `buttermilk-fried-chicken` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `buttermilk-pancakes` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `chicken-fajitas` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `chilli-jam` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `chocolate-layer-cake` (baking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `classic-brownies` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `cornbread` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `cornish-pasty` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `cranberry-sauce` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `croque-monsieur` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `falafel` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `flapjacks` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `fruit-scones` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `gooseberry-jam` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `granola` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `green-tomato-chutney` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `guacamole` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `iced-tea-southern` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `kibbeh` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `lemon-curd` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `lemon-drizzle-cake` (baking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `lemonade` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `lime-marmalade` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `margherita-pizza` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `meatloaf` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `mince-pies` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `new-england-lobster-roll` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `onion-bhaji` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `orange-marmalade` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `philly-cheesesteak` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `piccalilli` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `pierogi` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `pierogi-with-potato-and-cheese` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `pissaladiere` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `quick-pickled-red-onions` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `redcurrant-jelly` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `risotto-alla-milanese` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `sausage-roll` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `scottish-shortbread` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `seville-orange-marmalade` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `strawberry-jam` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `tarka-dal` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `treacle-tart-classic` (baking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `welsh-cakes` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `welsh-rarebit` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)

### `yorkshire-puddings` (cooking / RECIPE)
- **servings-yield** — both recipe.servings and recipe.yieldDescription are set — choose one (servings for portion-count yields, yieldDescription for discrete-item yields)


## Follow-ups for the audit-and-fix session

- For each `glossary-coverage` error: either register the missing term or inline-wrap the unused term.
- For each `servings-yield` error: pick exactly one of `servings` or `yieldDescription` per the rule in the v5 authoring appendix.
- For each `temperature-canonical` warning: confirm `recipe.temperatureCelsius` is conventional, not fan.
- For the freezable-without-notes count: either set `freezable: false` or write a one-line `freezeNotes`.
- For the missing-hero count: run the image-sourcing orchestrator across the slug list (pre-launch bulk fill).
