# Missing techniques — backlog for future authoring sessions

The upload script appends to this file when a recipe references a
technique slug via a `subTutorialCard` block that doesn't exist in
the database as a published `Tutorial`. A future technique-authoring
session walks this list and drafts the missing techniques.

Each entry:

```
- **{technique-slug}** — referenced by recipe `{recipe-slug}` on
  {date}. Suggested technique title: "{readable name}".
```

## Entries

<!-- Auto-appended below this line -->
