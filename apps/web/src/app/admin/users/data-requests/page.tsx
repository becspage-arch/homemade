import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminDataRequestsPage() {
  return (
    <AdminPlaceholder
      title="Data requests"
      description="GDPR subject-access requests and deletion queue. Wired up alongside the legal-pages session."
      phase="Legal pages follow-up (pre-public launch)"
    />
  )
}
