import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminMarketingPage() {
  return (
    <AdminPlaceholder
      title="Marketing"
      description="Email broadcasts, newsletter dispatch, in-app announcements, push notifications. Wired up once the launch lists exist."
      phase="Phase 8 — Premium tier and launch readiness"
    />
  )
}
