import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminAnalyticsPage() {
  return (
    <AdminPlaceholder
      title="Analytics"
      description="Pageviews, search-query log, funnels, cohort retention. Needs PostHog (or the chosen analytics provider) to be wired first."
      phase="Phase 8 — Premium tier and launch readiness"
    />
  )
}
