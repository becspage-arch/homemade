---
name: project_crochet_signoff
description: Live state of the crochet category sign-off pass (started 2026-06-22). Pattern-led rebuild; hero waits on the loom engine; target/taxonomy/template decisions locked with Rebecca.
metadata: 
  node_type: memory
  type: project
  originSessionId: 15d7383b-7b0e-4f4a-8ede-a2f947197712
---

Crochet sign-off pass, started 2026-06-22 (branch claude/inspiring-feistel-f1c2b8). Follows [[playbook_category_signoff]]. Builds on [[project_crochet_anchor]] (locked PATTERN template + chartData shape).

**CORRECTION + LOCKED METHOD (Rebecca, 2026-06-24).** Earlier "crochet only waits on the render" was OVER-OPTIMISTIC and wrong. Crochet has the SAME unsolved hard stack as needlework: (a) good CUTE diagrams/designs (past output was "ick" — a customer wouldn't use it), (b) diagram→exact-stitch FIDELITY (kept getting wrong, couldn't verify), (c) a trustworthy RENDER (bad output might've been render OR pattern — unverified), (d) GRADING, (e) photo/idea→pattern. Infrastructure (taxonomy/template/gating/planner/design-direction) is done; the core pattern-making engine is NOT. Also corrected: crochet items are NOT mostly flat — toys/amigurumi/garments are likely the MAJORITY (the bigger, harder chunk), sequenced later because hardest, not because small.
- **THE METHOD (identical to the embroidery loom; the other million approaches FAILED):** work BACKWARDS from a REAL, complete pattern; read EVERY stitch exactly (no summarising/guessing — that was a downfall); render the EXACT stitches as geometry → Blender path-trace to photographic. Never generate-our-own-then-render-blind (the downfall). Two SEPARATE problems: Problem A = the RENDER (prove first, on real patterns), Problem B = GENERATION of our own cute originals (separate/harder, after the render + needlework's generation approach land; even then transcribe every generated stitch exact, never render blind).
- **SEQUENCE (Rebecca):** 2D crochet → 2D knitting → 3D crochet → 3D knitting (macramé after — sibling craft, knots-on-cord not loops, mostly flat wall-hangings/plant-hangers, fewer patterns, strong SEO, reuses the render-engine pattern). Each stage: render-proof (work backwards from a real fixture) → pattern page + world-class instructions (format to be AGREED with Rebecca after reviewing the example-pattern folder) → Rebecca approves → THEN its bulk-authoring session runs in the BACKGROUND while the next stage starts.
- **FIXTURES (internal only, never shipped — like needlework's licensed embroidery fixtures):** external-designer patterns at `H:\My Drive\Example Patterns\Example Crochet Patterns\` (first proof = `Crochet Pattern - Aspen Throw.docx`, a flat blanket). Rebecca has knitting examples in another folder (to share) + will buy more as needed.
- **First worker fired 2026-06-24:** prove the 2D crochet yarn render on the Aspen Throw (Problem A only), extending the loom's proven Blender path-trace recipe with crochet loop-stitch primitives; offline proof on a branch.

**Live DB state at start.** Teaching already strong: 185 published (96 TECHNIQUE, 65 STITCH, 24 READING) — leave alone. Patterns are the gap: only 6 `CrochetPattern` rows (the grid + Studio read this table); 34 published PATTERN-type Tutorials (all have charts+heroes but only 6 have the CrochetPattern sidecar); 959 PATTERN Tutorials DRAFT (completeness-gate cull, 974 carry qcBlockReason): garments 330, homewares 218, amigurumi 200, lacework 110, motifs 101. Public `/crochet` grid + Studio key off `CrochetPattern` (currently ~6).

**Locked decisions (Rebecca, 2026-06-22):**
- Pattern-led; this pass = PATTERN rebuild + new. Teaching untouched.
- HERO = the loom engine ([[project_loom_render_engine]]); img2img killed; old crochet 2D renderer is "the bad one". Interim card = clean symbol chart / line drawing. Bulk + COMPLETE wait on the loom proving the doll; engine-independent build proceeds now.
- Amigurumi UN-DEFERRED (the 2D/3D line was wrong — garments are 3D too; the real blocker was the hero). Build amigurumi patterns + structured data now; hero waits on loom.
- "Whimsical crochet" is an INTRICACY LEVEL the engine must handle (fairy house, intricate dolls), NOT a category heading.
- Target ~1,200 (expect growth, like cross-stitch 500→thousands), demand-weighted, across a 13-category taxonomy (TBD/confirm with Rebecca): Blankets&Afghans, Sweaters&Cardigans, Tops&Dresses, Shawls&Wraps, Scarves&Cowls, Hats&Headwear, Gloves/Mittens/Legwear, Socks&Slippers, Bags&Accessories, Amigurumi&Toys, Home&Living, Kitchen&Bath, Doilies/Lace/Motifs. Seasonal/occasion = cross-craft TAG not a shelf.
- EXPANDED template (the missing best-in-category sections, per major-site + Craft Yarn Council research): finished measurements up front, graded sizing (table + inline S/M/L), construction overview, multi-piece parts + "make 2" + build order, assembly separate from finishing, special-stitch mini-tutorials, fuller materials (named yarn+fibre+yardage-per-size+notions+substitutes), difficulty + optional video, pattern notes (repeat/turning-chain/US-UK), schematic slot. Schema was already rich (sizesGraded, yardageBySize, pieces, buildOrder, assemblyInstructions, construction, schematicMediaId, difficulty, estimatedHours, specialStitchesUsed). 

**Migration applied 2026-06-22** (`20260920000000_crochet_pattern_expanded_template_fields`, applied directly via pg/non-pooled URL + committed to branch, idempotent ADD COLUMN IF NOT EXISTS): `CrochetPattern.videoUrl TEXT?` (nullable — designers may add, ours ship without initially), `notions TEXT[]`, `safetyNotes TEXT?`.

**Structured-data contract (shared with the loom session).** Flat/single-piece (blankets, motifs, doilies, edgings, scarves) = a single ChartDefinition (`apps/web/src/lib/craft-charts/types.ts`, round/flat) — fully covered now. Multi-piece 3D (amigurumi, garments, bags, hats) needs a per-piece structured stitch program (each piece: rounds/rows of stitch ops + colour changes + shaping, plus buildOrder + assembly) — today's ChartDefinition is single-piece; this is the shared contract to co-design with the loom session, drafted on the doll proof pattern. CrochetPattern already has `pieces`/`buildOrder`/`bodyShape`.

**Originals-dominant mix (Rebecca, 2026-06-22).** PD/vintage is the SMALL heritage slice (traditional motifs, doilies, edgings, vintage throws); the BULK + the heart of the library is Homemade-ORIGINAL — beautiful, fun, whimsical, fresh, stunning. PD won't deliver that bar. So the original count is the large number and the ~1,200 target is a FLOOR; the real ceiling is "how many genuinely stunning originals we can design + render," grows well past it over time (like cross-stitch 500→thousands). Originals = Claude-authored concept + structured stitch engineering (amigurumi shape-math + grading) + loom render to judge; NO AI image gen. This leans even harder on the loom (can't judge/sell an original without seeing the rendered make). Step 5 design-direction system (looks + palette library + creative briefs + originality rule + whimsy bar) is the engine-independent prep that determines whether bulk originals come out stunning vs generic — being developed now.

**Item-type taxonomy + AUDIENCE axis RATIFIED (orchestrator, 2026-06-22)** — see [[project_cross_craft_item_type]]. Built once centrally; crochet maps its ~45-type subset on once that session lands. Crochet HOLDS shelf creation until then.

**Planner adapter SHIPPED (on branch).** `apps/web/src/lib/planner/providers/crochet.ts` + one registry line — yarn ball-count from yardageBySize + CYC ball-metres table + 15% margin, size selection, stash by yarn weight. Core picks it up via registry; no core changes. "Add to planner" button on crochet patterns = UI for the premium step. Teaching coverage CONFIRMED (161 STITCH/TECHNIQUE + 24 READING; every renderable stitch symbol has a teaching entry; no gaps). Source list approved (PD heritage minority + CC0 reference + Homemade-original bulk; no AI/paid).

**Design direction (Step 5) SHIPPED** — shared, see [[project_cross_craft_design_direction]] (`packages/db/prisma/design-direction.ts`). Crochet starter brief set: `packages/db/scripts/crochet-signoff/starter-briefs.ts` (36 briefs, all 8 looks, 10/12 core territories, 31 item-types, full size/difficulty spread, doll showpiece first). Bulk extends it once the loom lands.

**Premium gating SHIPPED (on branch).** REUSABLE cross-craft gate added to `apps/web/src/lib/entitlements.ts`: `isPremiumContent` / `isIndependentDesignerContent` / `canAccessPremiumContent` — content is premium if `premium` flag OR non-house designer (independent/creator content, the designer-revenue library). All crafts + categories consume it. Crochet wiring: print/download page (`studio/crochet/[slug]/print`) gated on `hasPremium` (universal print=premium); Studio working surfaces (pattern open + create modes) behind `StudioAuthGate` (free login), mirroring cross-stitch; planner adapter (premium). FLAGGED heavy features (own sessions, scaffolding at `apps/web/src/lib/crochet/grading/`): grading engine + design-a-pattern builder. Still UI-pending (needs the pattern detail route/grid that arrive with bulk): preview+PremiumBadge on the crochet pattern page/grid via `isPremiumContent`; add-to-planner button. Orchestrator to spawn a worker to apply `isPremiumContent` consistently to cooking/baking/cross-stitch + brief the needlework worker.

**Item-type shelves LAID + content mapped (2026-06-22).** Central taxonomy shipped ([[project_cross_craft_item_type]]); rebased onto it. Ran `seed-item-type-shelves.ts crochet crochet --apply` → 57 crochet SubCategory shelves (56 created + amigurumi reconciled). Re-homed the 6 public CrochetPatterns + 18 published PATTERN tutorials: old `motifs`→`motif-granny-square`, `lacework`→`edging`. Old pattern sub-cats (homewares/garments/seasonal) + their 959 DRAFTs left for the loom-gated content rebuild to re-home per-item; `foundations`/`stitches` stay as teaching shelves. `categories.ts` crochet entry added (CROCHET_CHART, target 1200, shelves derived from `itemTypesForCraft('crochet')`, tierGuide `{}` — design-direction drives generation). Canonical garment slugs differ slightly from the draft (vocabulary has `tee-top`, `vest`, `jumper-pullover`; no `tank-top`/`tee`/`top`) — starter briefs reconciled. SEARCH REINDEX PENDING: the re-home changed `subCategorySlug` on those rows but raw updateMany doesn't trigger sync; cross-craft search is stale for them until a reindex from CURRENT code (do NOT run `search:backfill` from the behind MAIN checkout — it would regress the index, dropping audienceSlugs). The audience-backfill / spawned worker / next deploy reindex corrects it; DB-driven grid is correct now.

**Run-from-where gotcha (item-type tooling).** Worktree DB scripts can't run (Prisma client resolves to the wrong node_modules — `C:\Users\Rebecca\node_modules`). The seed/re-home were run by temporarily copying `prisma/item-type-vocabulary.ts` + `src/item-types.ts` into the MAIN checkout, running via `pnpm --filter @homemade/db exec tsx`, then removing the copies. Cleaner long-term: bring the main checkout up to origin/main (it's behind, has stale dupe "ahead" commits + untracked scratch — left untouched).

**THREE-LAYER BOUNDARY (orchestrator clarification 2026-06-22, now STANDARD for every pattern/project category, extends to woodwork etc. later).** The category OWNS GENERATION; the loom is RENDER-ONLY. Crochet turns a brief → its structured stitch data (rowsStructured, chartData, amigurumi shape-math, schematic) and feeds the loom via the contract; the loom only draws the finished-piece hero + line drawing FROM that data — it never designs. So: do NOT wait on the loom to author patterns, and do NOT build a render stack. Author makeable patterns with COMPLETE structured stitch data + charts now; the loom backfills render-grade heroes once it proves the contract. Only the render-grade HERO + BULK-to-target + COMPLETE wait on the loom's contract. UX stays seamless because the Studio edits the shared structured doc live and the loom renders it live (internal layers, not user steps). (Corrects the earlier "crochet is idle until the loom" framing — FLAT pattern generation uses the stable existing ChartDefinition contract and is authorable + publishable NOW with the chart as interim card, zero loom/contract-rework risk; only 3D/amigurumi per-piece structured data waits on the loom locking that part of the contract.)

**REORDERED 2026-06-22 — crochet now WAITS for the loom before any content (Rebecca).** She will not trust pattern quality until she sees a finished-look RENDER (past iterations read fine as data but came out clunky; only caught on render — see [[feedback_render_before_volume]]). So we do NOT author pattern volume blind, even though generation is technically unblocked. New plan: loom ready → author ~6 samples → render each via the loom → Rebecca approves/edits the finished-look images → only then bulk. One QC-passed candidate sample already exists unpublished (`crochet-wildflower-granny-square.json`, passes voice+completeness+makeability); NOT published (she sees the render first). Charts/code are never review artifacts for her — show finished-look images only. This overrides the orchestrator "keep authoring now" guidance on build-order.

**MERGED + DEPLOYED 2026-06-22** (main @ 4f99e2ac, deploy green, /healthz 200). On rebase the `isPremiumContent` helper conflicted — the spawned worker had already shipped it (+ wired cross-stitch/cooking/baking) to origin/main, so resolved to theirs (dropped my dup). One build fix: planner `resolveYardage` param must accept `string|null|undefined` (settings.size is optional) — `next build` typecheck caught it. (Deploy auto-applied migration `20260921000000_crochet_pattern_expanded_template_fields` as a no-op via IF NOT EXISTS.)

**Progress.** Step 0 ✔. Research ✔. Loom decision ✔. Step 1 migration ✔. Planner adapter ✔. Teaching coverage (Step 4) ✔. Source list (Step 3) ✔. Design direction (Step 5) ✔. Premium gating ✔. Item-type shelves + public-content mapping + categories.ts entry ✔. Merged + deployed ✔. WAITING on: loom contract before bulk authoring + heroes; THEN search reindex, pattern-page preview/badge, draft re-home, Step E vision sweep, COMPLETE.

Gotchas: DB tooling from MAIN checkout (worktree has no node_modules); `pg` not resolvable at root — run via `pnpm --filter @homemade/db exec tsx`; non-pooled URL (strip `-pooler`) for DDL; load DATABASE_URL from `.env.credentials` directly. Main checkout has the (paused) needlework worker's untracked hero-iter files — don't touch.
